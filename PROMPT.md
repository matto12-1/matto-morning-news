# RALPH LOOP — 데일리 문해력 신문 완성 미션

너는 이 저장소에서 **"오늘의 아침신문"(초등 3~6학년 데일리 문해력 신문)**을 만든다.
이 프롬프트는 **매 회차 똑같이** 주어진다. 너는 이전 회차의 결과물(파일 + git 이력 + `SCORES/`)을 보고 **스스로 이어서 개선**한다. 아래 **정지 조건**에 도달하면, 그리고 오직 그때만, 완료 태그를 출력한다.

## 정본 문서 (매 회차 반드시 참조)
- 스펙: `docs/superpowers/specs/2026-07-21-daily-literacy-newspaper-design.md`
- 계획: `docs/superpowers/plans/2026-07-21-daily-literacy-newspaper.md`
- 지난 채점: `SCORES/iteration-*.md` (가장 최근 것이 이번 회차의 출발점)

## 절대 규칙 (어기면 실패)
1. **증거 없이는 주장하지 않는다.** "동작한다"는 말은 실제로 `/browse` 스킬로 열어 클릭해보고, 테스트를 돌려본 뒤에만 한다. 스크린샷·테스트 출력이 증거다.
2. **점수를 조작해 탈출하지 않는다.** 너는 너 자신의 가장 혹독한 심사관이다. 애매하면 **낮게** 준다. 거짓 9점으로 `<promise>`를 내면 미션 실패다.
3. **백엔드·프레임워크·번들러 금지.** 빌드 스텝 없는 정적 파일만. (스펙/계획의 Global Constraints 준수)
4. **브라우징·검증은 gstack `/browse` 스킬만.** `mcp__claude-in-chrome__*` 절대 금지.
5. **콘텐츠는 네가(Claude) 집필**하고 `node tools/validate.mjs`를 통과해야 한다. 외부 LLM/이미지 API 안 쓴다(삽화 없음, `illustration:null`).
6. **회귀 금지.** 이번 회차에 이미 되던 기능을 깨뜨리면 안 된다. 매 회차 전 기능을 재검증한다.
7. **작은 커밋을 자주.** 각 의미 단위마다 커밋.

---

## 매 회차 프로토콜 (이 순서를 지켜라)

### 0단계 — 방향 잡기 (ORIENT)
- `git log --oneline | head`, `SCORES/` 최신 파일, 계획 문서를 읽어 **현재 회차 번호 N**과 **직전 backlog**를 파악한다.
- `SCORES/`가 비어 있으면 이번이 **1회차**: 계획(plan)의 Task 1~12를 실행해 **뼈대**를 만든다.
- 있으면 **직전 회차의 P0→P1→P2 backlog와 가장 낮은 점수 항목**을 이번 회차의 작업 대상으로 삼는다.

### 1단계 — 개선 실행 (BUILD)
- 이번 회차 대상 이슈를 실제로 구현/수정한다. 가중치 높은(=아동 UX, 아침 분량) 항목을 우선한다.
- 순수 로직을 바꾸면 `tools/`의 테스트도 갱신(TDD). 기능을 넓히면 검증도 넓힌다.

### 2단계 — 자가 실검증 (VERIFY, 에이전트로 직접)
반드시 실제로 실행한다. 상상 금지.
1. `node --test tools/` → **전부 PASS** 확인(출력 첨부).
2. `node serve.mjs` 로 로컬 서버 기동.
3. `/browse` 스킬로 `http://localhost:8080` 열고 **전 플로우**를 클릭:
   홈 로딩 → 난이도 토글(저/고) → 어휘 툴팁 → TTS 재생/정지 → 퀴즈(내용·어휘·생각·보너스, 정답·오답 모두) → 해설·리워드 → 인쇄 미리보기(학생/교사 × 저/고) → `?date=`(교사 접근) → 에러 폴백("신문이 쉬어가요").
4. **3개 뷰포트**(모바일 375 / 태블릿 768 / PC 1280)에서 반복. 각 뷰포트 스크린샷을 증거로 남긴다.
5. **아침 독서 분량 측정**: 저학년/고학년 본문 글자 수, 예상 읽기 시간, 읽기+퀴즈 총 소요를 실제로 계산해 기록한다.

### 3단계 — 다각도 채점 (SCORE)
아래 **루브릭**으로 8개 항목을 0~10점으로 매기고, **각 점수마다 근거(스크린샷/수치/테스트)**를 붙인다. 근거 없는 점수는 무효(=0 취급).
`SCORES/iteration-N.md`에 다음을 기록한다:
- 항목별 점수 + 근거
- 가중 합계(아래 가중치)
- 발견한 이슈를 **P0(치명)/P1(중요)/P2(개선)** 으로 분류
- **다음 회차 backlog**(우선순위 순)
- 정지 조건 충족 여부 판정

### 4단계 — 정지 판정 (DECIDE)
- **정지 조건 전부 충족** → `<promise>MORNING_PAPER_COMPLETE</promise>` 출력하고 종료.
- 아니면 → 이번 회차 요약만 남기고 종료(루프가 다시 이 프롬프트를 준다).

---

## 채점 루브릭 — **8개 영역을 각각 0~100점으로 따로 평가**

> **가중 평균 없음. 영역별 독립 100점 만점.** 한 영역이 잘한다고 다른 영역의 부족을 메울 수 없다(보상 금지). 미션의 강함은 **가장 낮은 영역**이 결정한다(약한 고리 원칙).
> 각 항목의 서술 앵커는 **10 = 만점(100점) 수준, 5 = 중간(50점) 수준**으로 읽어 0~100점으로 환산한다.
> 그래서 '내용(③④)'도 다른 모든 영역처럼 **혼자서 95점 이상**을 받아내야 한다 — 좋은 UX로 약한 내용을 덮는 것은 원천 불가.

### ③ 문해력 교육 효과 — 0~100점 (독립 평가)
어휘·내용·추론 퀴즈와 본문이 실제로 읽기 능력을 키우는가. **이 앱의 존재 이유.**
- 10: 본문이 흥미롭고 배울 게 분명, 어휘 문항이 본문 핵심어와 연결·수준 적절, 내용 문항이 단순 회상 넘어 이해·추론·적용 포함, 해설이 진짜 배움을 준다, 생각 넓히기가 사고를 확장, 저/고 난이도 차가 실질적.
- 감점: 문항이 본문만 훑으면 답하는 표피적 회상, 해설이 부실, 난이도 차가 유명무실.
- 근거: 문항·해설 인용 + 난이도 대비 분석.

### ④ 콘텐츠 품질·안전·중복없음 — 0~100점 (독립 평가)
- 10: 사실 정확, 안전 주제, 다정하고 흥미로운 어조, 아이 눈높이의 좋은 예시·비유, `validate.mjs` 통과, `topics.json`과 주제 중복 없음, `source` 명시.
- 감점: 사실 오류·과장, 지루하거나 어른스러운 어조, 근거 불명.
- 근거: 검증 출력 + 본문 발췌.

### ① 아동 UI/UX — 0~100점 (독립 평가)
초등 3학년이 **혼자, 설명 없이** 홈→읽기→퀴즈→완료까지 막힘없이 도달하는가.
- 10: 터치 타깃 큼(≥44px), 화면당 다음 행동 1개로 명확, 막다른 길 없음, 시각 위계 뚜렷, 피드백이 즉각·즐거움, UI 텍스트 최소, 색·아이콘으로 직관.
- 5: 성인은 쉽지만 아이가 헷갈릴 지점 존재.
- 근거: 3개 뷰포트 스크린샷 + "3학년 단독 완주 워크스루" 서술.

### ② 아침 독서 대체 분량·시간 적합성 — 0~100점 (독립 평가)
아침 독서 시간을 대체할 **딱 맞는 분량**인가. 너무 길면 집중 이탈, 너무 짧으면 효과 없음.
- 목표: 저학년 본문 **200~350자(≈2분)**, 고학년 **350~600자(≈3분)**. 읽기+퀴즈 총 **5~10분**.
- 10: 두 난이도 모두 목표 범위, 퀴즈 문항 수 과하지 않음(내용3~4·어휘2~3·생각1), 체감 리듬이 "아침에 딱".
- 감점: 범위 이탈, 퀴즈 과다로 지침, 저/고 분량 차이가 유명무실.
- 근거: 실제 글자 수·예상 시간 수치.

### ⑤ 기능 완결성 — 0~100점 (독립 평가)
스펙 §7 전 기능이 실제 동작: 홈/제호/난이도토글/어휘툴팁/TTS/퀴즈 즉시채점·해설·리워드/인쇄(학생·교사)/`?date=`/에러폴백.
- 근거: 플로우별 스크린샷.

### ⑥ 인쇄 워크시트 품질 — 0~100점 (독립 평가)
- 10: A4에 깔끔히 안착(넘침·잘림 없음), 학생용/교사용(정답·해설) 분리, 저/고 선택, 화면 UI 숨김, 잉크 절약.
- 근거: 인쇄 미리보기 스크린샷 4종(학생·교사 × 저·고).

### ⑦ 반응형·접근성 — 0~100점 (독립 평가)
- 10: 3개 뷰포트 깨짐 없음, 큰글씨 토글 동작, 대비 충분(WCAG AA), 키보드 조작 가능, TTS 지원/미지원 처리.
- 근거: 뷰포트 스크린샷 + 접근성 점검 노트.

### ⑧ 견고성·에러처리·코드품질 — 0~100점 (독립 평가)
- 10: 오늘 글 없을 때 폴백, 잘못된 JSON에도 앱 안 죽음, 콘솔 에러 0, 파일 책임 분리 유지, 테스트 그린.
- 근거: 콘솔 로그 + 테스트 출력.

---

## 정지 조건 (빡세게 — 전부 충족해야 `<promise>`)
1. **8개 영역 모두 ≥ 95 / 100** (영역별 독립. 하나라도 95 미만이면 미완 — 가중 평균으로 못 메운다).
2. **최저 영역 점수 ≥ 95** (1번과 같은 뜻, 약한 고리 강조).
3. **직전 회차에서도 위 1·2를 충족**(= **2회 연속** 통과, 우연·과대평가 방지).
4. **새로 발견된 P0·P1 이슈 0개** (이번 회차 자가 검증에서).
5. **`node --test tools/` 전부 PASS**, 콘솔 에러 0.
6. **`/browse` 전 플로우 × 3개 뷰포트** 실검증 통과(스크린샷 증거 존재).

## 정체(plateau) 처리
- 3회 연속 **최저 영역 점수** 향상이 +2 미만인데 아직 정지 조건 미달이면: **접근을 바꿔라.** 가장 낮은 두 영역을 근본부터 재설계(레이아웃/카피/문항 구조)하고, `SCORES/iteration-N.md`에 "정체 → 재설계 시도" 사유를 남긴다.

## 스코어카드 템플릿 (`SCORES/iteration-N.md`)
```md
# Iteration N — 2026-..-..
## 이번 회차 작업
- (한 일 요약, 커밋 해시)
## 자가 검증 증거
- 테스트: (node --test 출력 요약)
- 뷰포트 스크린샷: (경로/설명)
- 분량 측정: 저학년 ___자(≈__분) / 고학년 ___자(≈__분) / 총 소요 __분
## 점수 (근거 필수)
| 영역 | 점수(/100) | 근거 |
|---|---|---|
| ③ 문해력 효과 | _/100 | … |
| ④ 콘텐츠 품질·안전 | _/100 | … |
| ① 아동 UI/UX | _/100 | … |
| ② 아침 분량 | _/100 | … |
| ⑤ 기능 완결성 | _/100 | … |
| ⑥ 인쇄 품질 | _/100 | … |
| ⑦ 반응형·접근성 | _/100 | … |
| ⑧ 견고성·코드 | _/100 | … |
**최저 영역 점수: __ / 100  ·  95 미만 영역: [ ]**
## 이슈
- P0: …
- P1: …
- P2: …
## 다음 회차 backlog (우선순위 순)
1. …
## 정지 조건 판정
- 8영역 모두 ≥95? / 2회 연속? / P0·P1 0? / 테스트 PASS? / 실검증? → (계속 / 정지)
```

---

## 기억하라
- 1회차는 **뼈대**를 세운다(계획 Task 1~12). 완벽하지 않아도 된다 — 다음 회차가 있다.
- 매 회차 **가장 아픈 곳(= 최저 점수 영역)**부터 고친다. 그게 정지를 막는 병목이다.
- **아이가 아침에 혼자, 즐겁게, 딱 알맞은 분량을** 읽고 풀 수 있는가 — 이 질문이 모든 판단의 기준이다.
- 정지 조건에 진짜로 도달하기 전에는 절대 `<promise>`를 내지 마라.
