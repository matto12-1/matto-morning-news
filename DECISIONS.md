# 결정 대장

> 이 프로젝트의 확정 사항. **최신이 맨 위.** 작업 시작 전 반드시 여기부터 읽는다.
> 결정을 뒤집으면 새 항목을 위에 추가하고, 뒤집힌 항목에 ~~취소선~~과 사유를 남긴다.
> 세션 종료 시 자동 기록됨 (session-capture 훅). 수동 추가도 환영.


## 2026-09-10 — 다음 달 치를 **매달 1일 새벽 4시에 자동으로 만들어 PR로 올린다**. 합치기(발행)는 Matto가 한다

**이유**: Matto가 "매달 1일마다 다음 달 거 생성하게 하고 싶다. 내가 까먹을 수 있으니까". 10월치를 만들며 정한 절차(편성 → 집필 → 표지 → 검수 두 번 → 테스트)를 사람 없이 돌게 한다.

**구조**
- 작업 스케줄러 `MorningNewsMonthly` — 매일 04:00 실행, **1~5일에만 일한다**(사용 한도에 끊기면 다음 날 파일 상태에서 이어간다). PC가 꺼져 있었으면 켜는 순간 따라잡는다(GraphifySync와 같은 설정).
- `tools/monthly-run.ps1` — `_auto/morningNEWs-monthly`에 **따로 작업 복사본(git worktree)**을 만들어 `morning/YYYY-MM` 가지에서 일한다. **본 폴더와 master는 건드리지 않는다.**
- 창 없는 Claude(`claude -p`)가 `monthly-prompt.md`를 따른다. **Claude는 파일만 만들고 git·PR은 스크립트가 한다** — master에 올릴 경로가 아예 없다. 허용 도구도 파일 편집·node·npm·읽기 전용 git으로 좁혔다.
- 검사가 전부 통과해야(`.monthly/DONE`) PR을 연다. 판단이 필요한 건 멈추지 않고 PR 본문 「Matto 판단 필요」에 남긴다.
- 새 도구: `tools/check-month.mjs`(한 달 치 엄격 검사 — 합쇼체 0·밑줄 8/8·학년 간 중복 0·빈칸 겹침 0·퀴즈 형식) · `tools/holidays-kr.json`(평일 공휴일, 2027년 말까지).

**왜 끝까지 자동 배포하지 않나**: 10월치를 두 번 검수해 **치명 13건**이 나왔고, 69호처럼 사람이 방향을 정해야 한 것도 있었다. PR은 그 "한 번 보기"를 버튼 하나로 줄인 장치다. 기사가 다음 달 날짜라 합칠 시간은 한 달 있다.

**왜 클라우드가 아니라 이 PC인가**: 표지 생성에 Gemini 키가 필요한데, 클라우드 예약 실행에 넘기려면 프롬프트나 저장소에 넣어야 하고 **이 저장소는 공개**다. 키는 PC의 사용자 환경변수에만 둔다.

**알림은 캘린더로**: **GitHub은 본인이 만든 PR을 본인에게 알리지 않는다** — 자동 작업이 Matto 계정으로 PR을 여니 메일이 안 온다. 그래서 매달 2일 아침 구글 캘린더 알림(PR 목록 링크)을 따로 걸었다.

**⚠ 잊지 말 것**
- `tools/holidays-kr.json`은 **2027년 12월까지**다. 2027년 가을에 2028년을 채워야 한다(설날·추석은 음력).
- `2027-06-07`(현충일 대체공휴일)은 출처끼리 엇갈려 **확인필요** — 배치가 그 달에 웹으로 재확인한다.
- 합친 뒤 본 폴더에서 세션을 열면 `git pull`부터(원격 master가 앞서 있다).

**뒤집음**: `nightly-prompt.md`의 "매일 밤 한 편" 운영 — 실제로 돈 적이 없다. 문서 머리말에 취소선을 긋고, 집필 규칙 정본으로만 남겼다.

---

## 2026-09-10 — 10월치 **2차 원고 검수**: 1차가 놓친 치명 2건 + 1차 수정이 만든 새 오류들. **월간 배치는 검수를 두 번 돌린다**

**방법**: 1차 수정이 끝난 뒤 **새 검수자**에게, **편 묶음을 섞어서** 다시 맡겼다(한 편을 1차와 다른 눈이 보게). 초점은 ① 수정이 새로 만든 문제 ② 1차가 놓친 것. "뭔가 찾아야 한다"는 압박에 없는 문제를 만들지 않게 **"깨끗하면 '이상 없음'이 좋은 결과"**라고 못 박았다.

**결과: 치명 2 / 보통 27 / 경미 39** — 1차(치명 11)보다 적지만 0이 아니었다.

**1차가 놓친 치명 2건**
- **68 거북선**: 뱃머리를 "거북 머리"라 했는데 이순신 장계(1592)에는 **"앞에는 용머리를 붙이고(前設龍頭)"**. 거북 머리는 약 200년 뒤 『이충무공전서』(1795)의 기록이다. 1·2학년 OX 정답까지 거북 머리였다. **1차 검수자는 이걸 "전해져요로 잘 완충했다"고 칭찬하고 넘겼다** — 완충 표현이 붙어 있으면 사실 확인을 건너뛰기 쉽다.
- **69 금도끼 은도끼**: 기사 논지 "옛이야기는 누가 옳은지 말해 주지 않는다"가 원작과 어긋났다 — 산신령은 "정직하구나" 칭찬하며 도끼 세 자루를 주고, 이웃은 제 도끼마저 잃는다. 게다가 **원작은 이솝 우화 「헤르메스와 나무꾼」이고 1896년 『신정 심상소학』으로 들어와 전래동화처럼 자리 잡은 것**이다. 퀴즈 3문항이 이 논지에 묶여 있었다.
  - **원인은 나(메인 세션)였다.** 53호 피노키오와 '정직'이 겹치지 않게 축을 '옛이야기의 짜임새'로 틀면서, 원작에 없는 주장을 끼워 넣게 만들었다. **겹침을 피하려고 기사 축을 틀 때는, 튼 축이 원작 사실과 맞는지 먼저 확인할 것.**
  - **결정 (Matto 확정)**: 짜임새 축은 유지하고 사실만 바로잡는다 — "산신령은 한마디 칭찬할 뿐, 거짓말이 왜 나쁜지 길게 풀어 가르치지 않는다"로 논지를 좁히고, 결말을 사실대로 넣고, 알아두기에 **이솝 기원**을 넣었다. 제목 "왜 하필 세 번일까?"는 그대로.

**1차 수정이 만든 새 오류** — "고친 사람은 자기 수정을 못 본다"가 실제로 확인됐다
- **59 꿀벌**: 1차에서 꽃 구조를 바로잡으며 넣은 "사과는 한 송이에 두 부분이 있어서…"가 **사과는 벌이 덜 필요하다**는 뉘앙스를 만들었다. 사실 사과는 다른 나무 꽃가루가 필요해 벌에 가장 많이 기대는 작물이다.
- **가장 흔한 유형 — 한 곳만 고치고 옆은 옛 그대로**: 51(비누 설명은 고쳤는데 소제목은 옛 약속), 62(바닥 색을 고쳤는데 다음 문단·퀴즈는 "길을 노랗게"), 67(본문은 "빨강은 새로 만든다", 알아두기는 "잎에 든 색"), 70(1·2학년 퀴즈만 "새 실내화"). → 2차 수정 지시에 **"같은 내용이 나오는 모든 곳을 grep으로 찾아 함께 고쳐라"**를 못 박았고, 22개 지점을 원문에서 직접 확인했다.

**교훈 — 다음 달부터 이렇게 한다**
1. **20편 배치는 검수를 두 번 돌린다.** 1차 → 수정 → **새 검수자·섞은 묶음**으로 2차 → 수정. 1차 치명 11 → 2차 치명 2로 줄었지만, 2차를 안 했으면 용머리와 이솝이 그대로 나갔다.
2. 수정 지시에는 항상 **"한 곳 고치면 같은 내용 전부 grep"**을 넣는다.
3. 검수자가 **"완충 표현으로 잘 처리했다"고 칭찬한 대목**도 사실 자체는 따로 확인한다.

**최종**: 20편 전 항목 통과 — 분량·validate·밑줄 8/8·합쇼체 0·'~랍니다' 보존·학년 간 중복 0·빈칸 겹침 0. `npm test` 62개, A4 인쇄 350건 전부 한 면.

---

## 2026-09-10 — 10월치 20편 전수 원고 검수: **치명 11건**. 병렬 집필이 계통적으로 깨뜨린 셋은 테스트로 고정했다

**방법**: 네 갈래로 나눠 한 편씩 정독(사실·퀴즈를 **직접 풀어 대조**·표현·어법·학년 적합성) + 기계 검사(분량·밑줄·문체·학년 간 중복·빈칸 정답·A4 인쇄 350건). 검수한 에이전트가 세부를 쥐고 있으니 **같은 에이전트에게 이어서 고치게** 했고, 끝난 뒤 내가 전 항목을 다시 쟀다.

**치명 11건 — 발행됐으면 아이에게 틀린 것을 가르쳤을 것들**
- 51 감기: "비누가 바이러스의 기름 막을 무너뜨린다" 단정 — 감기 주범 리노바이러스는 기름 외피가 없다. 하필 오개념을 *정정하는* 자리였고 퀴즈 정답까지 묶여 있었다.
- 59 꿀벌: "꽃 한 송이 안에 두 부분이 함께" 단정 — 같은 기사가 예로 든 오이·수박이 반례(암수 꽃이 따로 핀다).
- 60 큰 나라: 러시아 최대 시차는 10시간인데 "서쪽이 아침이면 동쪽은 이미 밤" — 본문·해설·순서 문제·소제목 **5곳**에 번져 순서 문제의 결론으로 굳어 있었다.
- 62 스쿨존: factbox는 시속 30km를 "어른 달리기의 서너 배", 본문은 "조금 빠르다" — 본문이 맞다(전력질주 ≈ 시속 20km).
- 66 건물: "건물의 절반쯤은 땅속" — 근거 없는 수치(초고층 기초는 높이의 10% 안팎).
- 69 금도끼: 3·4학년 본문·OX가 같은 호 5·6학년 본문과 **정면 모순**. 이야기를 아는 아이가 맞히고도 틀리게 채점된다.
- 53·63·64·65·70: 빈칸 문제 결함(빈칸 두 개에 정답 하나 / 넣으면 말이 겹치는 정답 / 다른 낱말 뜻풀이와 겹치는 문항).

**69호 제목 변경 (Matto 확정)**: "금도끼 은도끼, 정직이 준 선물" → **"금도끼 은도끼, 왜 하필 세 번일까?"**. 53호 피노키오와 '정직'이 겹쳐 기사 축을 '옛이야기의 짜임새'로 옮겼는데 제목만 남아 기사 논지("판단은 듣는 사람 몫")를 뒤집고 있었다. **기사 축을 바꾸면 제목도 같이 볼 것.**

**병렬 집필의 계통적 어긋남 셋 — 전부 수치로 재기 전엔 안 보였다**
1. **문체**: 딱딱한 합쇼체가 기존 50편 편당 0~4건 → 10월 편당 최대 33건.
2. **고학년 낱말 밑줄**: 평균 7.9 → 6.6개.
3. **빈칸 정답 겹침**: 10월 3편 + **이미 배포된 3·5·28호**에서도 발견("빨라서 때문에" 등). 기존 호도 같이 고쳤다.
→ `tools/archive.test.mjs`에 세 불변식을 추가했다(`npm test` 62개). `nightly-prompt.md`에도 규칙을 적었다.

**내 실수 하나 — 되살리기 쉬우니 적어 둔다**: 문체 탐지를 넓히다 `(니다|시다)`로 셌더니 기존 50편에서 831건이 걸렸다. 거의 전부 **"~랍니다/~답니다"**였다 — 이건 딱딱한 합쇼체가 아니라 **이 신문의 이야기체이고 지켜야 할 목소리**다. 이미 세 갈래에 그 패턴을 보낸 뒤라 긴급 정정했고, 수정 전후 '~랍니다' 개수를 기준값과 대조해 **한 건도 줄지 않았음**을 확인했다. 앞서 "기존 50편 합쇼체 0건"이라 한 것도 첫 측정이 9월만 보고 '합니다·부릅니다' 형태를 놓친 결과였다(실제 16건).
> **문체를 기계로 셀 때는 `~[답랍]니다`를 반드시 제외한다.**

**결과**: 20편 전 항목 통과 — 분량·validate·밑줄 8/8·합쇼체 0·'~랍니다' 보존·학년 간 중복 0·빈칸 겹침 0. A4 인쇄 350건 전부 한 면(61호 276 → 271mm).

---

## 2026-09-10 — 표지 일러스트 오염의 원인 셋을 찾아 코드로 막았다 (20장 중 8장 불합격 → 전부 회수)

**계기**: 10월치 20장을 생성해 네 갈래로 나눠 전수 육안 검수했더니 **8장 불합격**(40%). 확대해 보지 않았으면 절반은 놓쳤을 것들이다 — 간판·러그 로고·기차 측면의 문자 흉내는 전체 보기에서 안 보였다.

**원인이 하나가 아니라 셋이었다**:
1. **제목 끝 물음표가 그림에 글리프로 박힌다.** 이 신문 제목은 대개 물음표로 끝난다. 그 제목을 그대로 "그릴 개념"으로 넘기니 모델이 `?`를 그렸다(10-12·10-21). → `buildImagePrompt()`가 개념에서 `?`·`!`를 걷어낸다.
2. **계열 문구가 그림 주제를 덮어썼다.** art 계열을 전부 `"art and music"`으로 넘기고 있었는데, 이번에 예술 1칸을 넷으로 쪼개면서(그림·음악·공연·건축) **건축 기사에 음악회 그림**이 나왔다(10-26). 계열은 넓게만 알려 주고 무엇을 그릴지는 개념 줄이 정하게 했다. **큐를 넓히면 프롬프트의 계열 매핑도 같이 봐야 한다** — 이번엔 그걸 빠뜨렸다.
3. **글자가 주제인 기사는 제목을 넘기는 것 자체가 화근이다.** 55호('한글'이라는 이름)에서 **2026-08-07 사고와 똑같이** 깨진 한글 제목이 그림에 박혔다. 그때 강화한 "제목을 따옴표로 인용하지 말 것" 규칙으로도 못 막았다 — 인용을 안 해도 개념이 '한글'이면 모델은 글자를 그린다. → 기사 JSON에 **`imageConcept`**(영어로 그릴 장면을 직접 서술)를 넣으면 제목 대신 그걸 쓴다. 55·57·63호에 적용했다.

**교훈: 같은 사고가 재발했다는 것은 그때의 대책이 원인의 일부만 덮었다는 뜻이다.** 2026-08-07에는 '제목 인용'을 막았는데, 진짜 조건은 '개념이 문자와 관련되어 있음'이었다.

**같이 고친 것**: `gen-all-images.mjs`가 429·5xx에서 **아무것도 안 찍고** 재시도하고 있었다. 그래서 아래 지출 상한 문제를 레이트리밋으로 착각해 한참 헤맸다. 이제 상태 코드와 응답을 찍는다.

---

### 결말 — 20장 전부 합격 (같은 날 해결)

**중간에 막혔던 것**: `gemini-2.5-flash-image`가 **HTTP 429 `RESOURCE_EXHAUSTED` — "exceeded its monthly spending cap"** 로 멈췄다. **선불 잔액이 남아 있어도 막힌다** — 잔액과 **프로젝트 월 지출 상한**은 별개이고, 상한은 API 키가 아니라 **프로젝트 단위**라 게임 이미지 생성 등 다른 작업이 같은 상한을 함께 먹는다. 텍스트 모델까지 같이 429가 났다. Matto가 AI Studio에서 상한을 올려 풀었다. (Higgs 등으로 우회하지 않았다 — 전역 CLAUDE.md 규칙)

**고침이 실제로 통했는지**: 2차 생성에서 지난번 지적 부위가 전부 깨끗해졌다 — 10-08의 깨진 한글 제목(`imageConcept` 효과), 10-12·10-21의 물음표(`?` 제거 효과), 10-26의 음악회 그림(계열 문구 효과). **셋 다 원인 진단이 맞았다.**

**3연속 실패한 한 장이 남긴 교훈 — 10-07(광고)**: 태블릿 배지 → 시리얼 상자 "Fantastic" → 상자 옆면·로봇 가슴 패널로 **자리만 옮겨 가며** 계속 문자가 박혔다. 금지 문구를 아무리 늘려도 소용없었다. 통한 것은 **글자를 새길 표면 자체를 장면에서 없앤 것** — 상자·기계·선반·종이를 다 빼고 봉제 인형·리본·빛만 남기니 한 번에 깨끗해졌다.

> **모델에게 "글자를 쓰지 마라"고 말하는 것보다, 글자가 놓일 자리를 안 주는 편이 확실하다.**
> 오염이 두 번 이상 반복되면 금지 문구를 늘리지 말고 **소재를 바꿔라**.

`imageConcept`를 넣어 둔 편: 54(광고) · 55(한글 이름) · 57(큰 수) · 63(다르다/틀리다) · 66(건축).

---

## 2026-09-09 — 대주제 순환 큐를 **계열 균등 인터리브**로 재편한다 (과학 편중 해소) + 포인터는 이름이 아니라 `slot`으로 대조한다

**이유**: 10월치를 준비하며 Matto가 "특정 분야가 너무 많이 나온다"고 했다. 세어 보니 느낌이 아니라 구조였다 — 50편 중 과학 계열이 **15편(30%)**, 예술은 **2편(4%)**. 원인은 `content/domains.json`의 rotation 29칸 중 과학이 9칸, 예술이 1칸이었던 것. **편중은 집필이 아니라 큐에 박혀 있었다.**

**바꾼 것**: rotation을 **64칸**으로 다시 짰다. 계열별 정확히 8칸이고, **연속한 8칸이 8계열 하나씩** 도는 인터리브다(`_order` 순서 × 8바퀴). 이 불변식 덕에 **어느 2주를 잘라 내도 계열이 고르게 섞인다** — 월별로 손으로 배정할 필요가 없다.
- 보강한 대주제 8개: 시와 노랫말 · 헷갈리는 말 · 습관과 도전 · 로봇·기계, 그리고 '예술' 1칸을 **그림·명화 / 음악·악기 / 공연·무용 / 건축·디자인** 넷으로 쪼갬.
- `수와 퍼즐`은 과학 → 기술 계열로 옮김(과학 9칸을 8칸으로 맞추기 위해. 수학은 어느 쪽에 둬도 무방하다).
- 큐를 또 손볼 일이 있으면 **계열별 목록만 고치고 인터리브 규칙은 유지할 것.** 계열마다 대주제 수가 달라지면 8칸 불변식이 깨진다(현재 과학 8 / 나머지 4, 8바퀴로 딱 떨어진다).

**같이 드러난 사고**: `topics.json`의 `nextIndex`가 **22**였는데 실제 마지막 발행(9/30 예술)은 rotation **14**번이었다. 그대로 10월을 시작했으면 15~21번 대주제 **7개를 통째로 건너뛸** 뻔했다. 50편을 한 번에 만든 배치 커밋에서 포인터만 안 맞춰진 것으로 보인다.

**고침**: `nextIndex`를 48로. 그리고 **51호부터 `published[].slot`에 그 편이 쓴 rotation 인덱스를 기록**한다. 포인터 검사를 **대주제 이름으로 하면 큐를 재편할 때마다 깨지므로**(이번에 '예술'이 '그림·명화'로 바뀐 것처럼), 이름이 아니라 숫자로 대조한다.

**검사로 고정**: `tools/archive.test.mjs` 신설 — index↔파일 일치, issueNo 연속, 이력↔index 일치, slot↔대주제 일치, `nextIndex == (마지막 slot+1) % 길이`, 그리고 **8칸 인터리브 불변식**. `npm test`에 포함.

**교훈: "느낌상 편중"이라는 보고를 받으면 고르는 방식을 고치기 전에 큐를 세어 볼 것.** 10월 편성만 손으로 균등하게 짰다면 11월에 똑같은 보고를 다시 받았을 것이다.

---

## 2026-09-05 — 마또뉴스 기사에서 '분리수거' 대신 '분리배출'을 표준 용어로 쓴다 (배출자=독자가 주어일 때).

**이유**: 환경부·자원재활용법 공식 용어가 '분리배출'이고, 내놓는 사람이 주어이므로 收去(수거)가 아니라 排出(배출)이 맞다.

**뒤집음**: 제32호에서 '분리수거'로 표기했던 것

⚠️ 이전 결정이 기록된 스펙·문서에 취소선을 넣어야 좀비가 되살아나지 않는다.

---

## 2026-08-20 — 조회수는 Supabase로 세되, 전용 프로젝트가 아니라 기존 'toktok' 프로젝트에 스키마를 분리해 얹는다

**이유**: Supabase 무료 요금제는 **계정당** 활성 프로젝트 2개까지다(조직당이 아니다). 이미
toktok·Ys Ele로 2개를 써서 전용 프로젝트 생성이 400으로 거부됐다. 유료 전환이나 기존
프로젝트 정지 없이는 방법이 없어, 기존 프로젝트에 얹고 격리로 푼다.

**격리 방식**: 조회수 테이블은 `morningnews` 스키마에 두고 PostgREST에 노출하지 않는다.
바깥에 나가는 것은 `public.mn_bump_view()` / `public.mn_get_views()` 두 함수뿐이며 anon에게
실행 권한만 준다. toktok의 기존 테이블·함수·정책·API 설정은 하나도 건드리지 않았다
(PostgREST 설정을 바꾸면 재시작이 걸려 교사 플랫폼이 잠깐 끊긴다).

**anon key 공개 판단**: 아침신문 저장소는 공개다. toktok의 anon key가 거기 올라간다.
toktok이 이미 웹에 배포돼 그 키를 브라우저로 내보내고 있음을 확인해서, 새로 생기는 노출이
없다고 보고 진행했다. **toktok을 비공개로 되돌릴 일이 생기면 이 판단도 다시 봐야 한다.**

**남은 숙제(별건)**: toktok은 익명에게 14개 중 13개 테이블 읽기를 허용하고 있다
(`profiles`·`teacher_verification_requests`·`audit_log`·`reports` 포함, 쓰기는 차단됨).
조회수와 무관한 toktok 자체의 RLS 문제이니 그쪽에서 따로 점검할 것.

**세는 기준**: 탭(세션) 하나당 1회. 날짜는 한국 시간 기준(UTC면 밤 9시에 넘어간다).

---

## 2026-08-19 — 인디스쿨 쫑알쫑알 공유는 완전 자동 제출이 아니라 반자동(클립보드 복사 + 라운지 자동 열기 + 수동 Ctrl+V·등록)으로 간다

**이유**: 인디스쿨은 매번 수동 로그인이 필요하고, 계정 자격증명을 평문 저장해야 하며, 봇 게시는 도배·홍보 신고 위험이 있어 클릭 몇 번 아끼는 가치가 없음

---

## 2026-08-19 — Node 스크립트를 버리고 의존성 없는 PowerShell 단일 파일(tools/post-to-indischool.ps1)로 교체, 콘텐츠는 배포 사이트에서 직접 fetch

**이유**: 실제 사용처가 학교 PC라 저장소·Node가 없고, PowerShell은 모든 Windows에 기본 탑재

**뒤집음**: 처음 만든 tools/post-to-indischool.mjs (삭제됨)

⚠️ 이전 결정이 기록된 스펙·문서에 취소선을 넣어야 좀비가 되살아나지 않는다.

---

## 2026-08-19 — 북마클릿(입력창 자동 채우기)은 폐기하고 클립보드+Ctrl+V 방식만 유지

**이유**: 사용자 테스트에서 동작하지 않았고, Ctrl+V 한 번 아끼는 것에 비해 설정·유지 비용이 과함

**뒤집음**: 북마클릿으로 입력창을 자동 채우려던 시도 (tools/bookmarklet.txt 등 3개 파일 삭제)

⚠️ 이전 결정이 기록된 스펙·문서에 취소선을 넣어야 좀비가 되살아나지 않는다.

---

## 2026-08-19 — 게시글 포맷 확정: 📰 이모지 + '문해력 기초를 키우는 마또의 아침뉴스 제N호' / 빈 줄 / '오늘의 주제 : 제목' / 빈 줄 / 날짜 파라미터 포함 사이트 URL — 3줄 단문

**이유**: 쫑알쫑알은 제목 칸 없는 단문 잡담방이라 길고 격식 있는 글이 어색함

**뒤집음**: 제목+본문 분리형 및 학년 안내·소요시간 포함 긴 포맷

⚠️ 이전 결정이 기록된 스펙·문서에 취소선을 넣어야 좀비가 되살아나지 않는다.

---

## 2026-08-19 — 작업 스케줄러는 평일 08:10 + StartWhenAvailable 옵션으로 등록해 꺼져 있던 PC를 켜는 순간 실행되게 함

**이유**: 학교 PC는 8:10에 꺼져 있을 수 있어 놓친 작업 따라잡기가 필요

---

## 2026-08-19 — 학교 PC 배포는 깃허브 raw URL에서 irm 한 줄로 다운로드+Install하는 방식으로 하고 ps1을 저장소에 푸시함

**이유**: 사용자가 파일 옮기기를 어려워해 파워셸에 한 줄 붙여넣기가 가장 단순한 경로

**뒤집음**: 네이버 메일 첨부/USB로 파일을 직접 옮기는 방식

⚠️ 이전 결정이 기록된 스펙·문서에 취소선을 넣어야 좀비가 되살아나지 않는다.

---

