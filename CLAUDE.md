# 프로젝트 규칙 — 오늘의 아침신문

## 표지 일러스트 생성 규칙 (2026-08-07 사고 이후 필수)

**사고 경위:** `content/img/2026-08-07.jpg`에 영어 기사 제목("WHAT DOES 'WIDE FEET MEAN?")과
깨진 한글("발이 법다는 뮤슨 뚱까까")이 그림 위에 그대로 박혀서 발행 직전 발견됨.
원인은 이미지 프롬프트가 기사 제목을 따옴표로 그대로 인용해 모델에 넘겼던 것 — 모델이 그 문구를
캡션/간판처럼 그림에 그려 넣었고, 특히 한글 렌더링은 자모가 깨진 형태로 나왔다.
검수 결과 "MILK"(냉장고), "SCHOOL"(학교 건물), "Paper/Plastics/Cans"(분리수거통), 물음표(?) 기호,
알파벳 "X" 등 같은 부류의 텍스트/기호 오염이 다른 예정 이미지에서도 여러 건 발견되어 전부 재생성함.

### 규칙
1. **표지·삽화 이미지에는 어떤 형태의 글자·기호도 넣지 않는다.**
   한글, 영어, 숫자, 구두점 기호(물음표 `?`, 느낌표 `!`, X, O, 체크 등)를 포함해
   읽을 수 있는 모든 문자는 금지. 간판·라벨·포장·책 표지·화면·현수막 등에 글자가 필요해 보이는
   소재가 나오면 **글자 없이 비워두거나 다른 방향으로 돌려서** 그린다.
2. **이미지는 반드시 해당 기사의 핵심 내용과 시각적으로 연결되어야 한다.**
   아무 주제에나 붙여도 되는 예쁘기만 한 범용 그림 금지 — 독자가 그림만 보고 "이게 무슨 이야기인지"
   짐작할 수 있어야 한다.
3. **기사 제목을 프롬프트에 따옴표로 그대로 인용하지 않는다.** 제목/부제를 "그려야 할 개념"으로
   설명하되, 문구를 그대로 베끼지 말라고 명시적으로 지시한다. (텍스트 렌더링을 유발하는 가장 큰
   원인이 제목 인용이었음.)
4. 실제 생성 스크립트: `tools/gen-image.mjs`(1편), `tools/gen-all-images.mjs`(전체 배치) — 둘 다
   프롬프트를 `tools/img-prompt.mjs`의 `buildImagePrompt()`에서 공용으로 가져온다. **프롬프트를
   수정할 일이 있으면 이 파일 하나만 고치면 된다.** 개별 스크립트에 프롬프트 문자열을 직접
   중복해서 넣지 말 것.
5. 새 이미지를 생성한 뒤에는 **반드시 육안으로 열어서** 텍스트/기호 오염이 없는지, 기사 내용과
   관련 있는 그림인지 확인한다(Gemini는 "NO text" 지시를 지켜도 가끔 어길 수 있는 확률적 모델임 —
   지시만 믿지 말고 결과물을 확인). **의심 부위는 잘라서 확대해 볼 것** — 2026-09-10 검수에서
   간판·러그 로고·기차 측면의 문자 흉내는 전체 보기로는 안 보이고 확대해야 드러났다.

### 2026-09-10 검수에서 새로 드러난 것 (20장 중 8장 불합격)
- **제목 끝 물음표가 그림에 글리프로 박힌다.** 이 신문 제목은 대개 물음표로 끝나는데, 그 제목을
  개념으로 넘기면 모델이 `?`를 그려 넣는다. → `buildImagePrompt()`가 개념에서 `?`·`!`를 걷어낸다.
- **계열 문구가 그림 주제를 덮어쓴다.** art 계열을 전부 `"art and music"`으로 넘긴 탓에 건축 기사에
  음악회 그림이 나왔다. 계열은 넓게만 알려 주고 무엇을 그릴지는 개념 줄이 정하게 고쳤다.
- **글자가 주제인 기사는 제목을 아예 넘기면 안 된다.** 55호('한글'이라는 이름)에서 2026-08-07과
  똑같이 깨진 한글 제목이 그림에 박혔다. → 기사 JSON에 **`imageConcept`**(영어로 그릴 장면을 직접
  서술)을 넣으면 `buildImagePrompt()`가 제목 대신 그것을 쓴다. 우리말·낱말·시·수 같은 대주제는
  이 필드를 미리 채워 두는 편이 안전하다(55·57·63호에 적용됨).
6. 이미지 생성은 반드시 `GEMINI_API_KEY`(Google AI Gemini, `gemini-2.5-flash-image` / 나노바나나)로
   한다. Higgs 등 다른 MCP 이미지 도구로 우회하지 않는다(전역 CLAUDE.md 규칙과 동일).

### 향후 배포 예정 콘텐츠 감사 시
- 새 기사를 발행 전 대량으로 미리 생성해 둔 경우(`content/*.json` + `content/img/*.jpg`),
  발행 전에 이미지들을 표본이 아니라 **전수 검수**한다. 이미지 수가 많으면
  `superpowers:dispatching-parallel-agents` 패턴으로 여러 에이전트에 나눠 병렬 육안 검수한다.
- 문제 발견 시 해당 `content/img/YYYY-MM-DD.jpg`만 삭제 후
  `node tools/gen-all-images.mjs <넉넉한 LIMIT>`을 실행하면 누락분만 이어서 재생성된다
  (이미 있는 파일은 건너뜀).
