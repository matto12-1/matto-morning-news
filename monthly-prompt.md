# 월 배치 지침서 — 다음 달 치 아침신문을 만들어 PR로 올린다

> 이 문서는 **예약 작업(`tools/monthly-run.ps1`)이 창 없이 부르는 Claude**가 읽는다. 매달 1~5일 새벽 4시에 돈다.
> 사람이 세션에서 "다음 달 치 준비해줘"라고 할 때도 이 문서를 그대로 따르면 된다.
> 절차의 근거는 `DECISIONS.md` 2026-09-09 · 2026-09-10 항목들이다(10월치를 사람과 함께 만들며 확정한 것).

## 0. 이 작업의 성격 — 먼저 읽을 것

- **확정된 절차를 실행하는 일이다.** 브레인스토밍·계획 위임(Plan 에이전트)·스킬 호출·볼트 기록·`DECISIONS.md` 수정은 **하지 않는다.**
- **코드(`assets/`·`tools/`)는 고치지 않는다.** 기사 JSON·표지 그림·`index.json`·`topics.json`만 만든다.
- **git은 네가 하지 않는다.** 커밋·푸시·PR 생성은 실행 스크립트가 네가 끝난 뒤에 한다. 너는 파일만 만들고, 마지막에 `.monthly/` 안에 PR 제목·본문·완료 표시를 남긴다.
- **중간에 물어볼 사람이 없다.** 방향이 갈리는 판단(아래 7절)은 **임의로 정하지 말고** 가장 보수적인 안으로 두고 PR 본문 「Matto 판단 필요」에 적는다. 멈추지 말고 끝까지 간다.
- **에이전트(Agent 도구)는 반드시 `run_in_background: false`로 부르고, 한 메시지에 여러 개를 동시에 호출해 병렬로 돌린다.** 이 세션은 창 없이 돌아서, 백그라운드로 띄우면 세션이 먼저 끝나 결과를 잃는다. 한 번에 **최대 5개**.
- 프롬프트 맨 위에 스크립트가 적어 준 **대상 월·모드**를 따른다.

## 1. 이어하기 — 매번 여기서 시작

한도에 걸려 끊겼다가 다음 날 다시 불릴 수 있다. **파일 상태가 진실이다.**

1. `.monthly/progress.md`가 있으면 읽는다. 끝난 단계는 건너뛴다.
2. 없으면 만든다. 단계를 끝낼 때마다 `- [x] A 편성 (YYYY-MM-DD HH:MM)`처럼 한 줄씩 적는다.
3. 진행 기록과 파일이 어긋나 보이면 파일을 믿는다 — `node tools/check-month.mjs <대상 월>`로 재 보고 판단한다.

## 2. 먼저 읽을 것

`DECISIONS.md`(최신이 위) → `nightly-prompt.md`(집필 규칙 전문) → `CLAUDE.md`(표지 규칙) → `content/domains.json` · `content/topics.json` · `content/index.json` · `tools/holidays-kr.json`.
골드 정본 기사: `content/2026-10-01.json`(이번 규칙을 전부 통과한 최신 편).

## A. 편성 → `.monthly/plan.json`

- **발행일** = 대상 월의 평일(월~금) − `tools/holidays-kr.json`의 날. 방학 중에도 발행한다.
  - 대상 월 날짜가 목록에 하나도 없으면(새해 등) **웹으로 그 달 공휴일을 확인**하고, 쓴 날짜와 출처를 PR 「확인할 것」에 적는다. 목록 파일은 고치지 않는다.
  - 목록에 `확인필요`가 붙은 날이 대상 월에 있으면 웹으로 재확인하고 결과를 PR에 적는다.
- **순번**: 첫 편 `issueNo` = `index.json` 길이 + 1. 첫 편 `slot` = `topics.nextIndex`, 이후 한 편마다 +1 (64에서 0으로 돈다). 각 편의 `category`·`categoryLabel`·`badgeLabel`은 `domains.rotation[slot]`의 `theme`·`categoryLabel`·`domain`.
- **소주제(제목)** — 대주제 안에서 새로 정한다:
  - `topics.published` **전 편**의 제목·키워드와 겹치지 않게.
  - **같은 달 안에서 같은 교훈이 두 번 나오지 않게** (10월: 피노키오와 금도끼 은도끼가 둘 다 '정직' → 한쪽을 틀다가 원작과 어긋나는 사고가 났다). 겹치면 **소재를 바꾼다. 원작의 논지를 비틀어 피하지 않는다.**
  - 그 달의 절기·기념일이 있으면 살린다(예: 한글날 전날 '한글' 편, 단풍철 '단풍' 편).
  - 전쟁·재난·질병 공포·정치 논쟁·종교 교리·가정 형편 비교는 피한다.
- **글자가 주제인 편**(우리말·낱말의 유래·헷갈리는 말·사자성어·수와 퍼즐·광고·지도처럼 그림에 문자가 들어가기 쉬운 소재)은 이 단계에서 표지용 `imageConcept`(영어, 그릴 장면)를 **미리 정해** plan에 적는다. 규칙은 D단계.
- `plan.json` 형식: `[{ date, issueNo, slot, domain, theme, categoryLabel, title, angle(한 줄 방향), imageConcept? }]`

## B. 집필 — 에이전트 병렬, 5개씩

각 편마다 에이전트 하나. 지시에 반드시 넣을 것(10월에 이것들이 빠져서 전 편을 다시 고쳤다):

```
너는 "마또의 아침신문"(초등 문해력 신문)의 제{issueNo}호 기사 1편을 집필한다. 작업 폴더는 현재 폴더.
- 만들 파일: content/{date}.json — 이 파일 하나만. index.json·topics.json·domains.json·코드·git 금지. 이미지 생성 금지(illustration: null).
- date "{date}", issueNo {issueNo}, category "{theme}", categoryLabel "{categoryLabel}", badgeLabel "{domain}", 제목 "{title}", 방향: {angle}
- readingTimeMin { "sprout": 2, "lower": 4, "upper": 6 }
- 먼저 읽기: nightly-prompt.md(집필 규칙 전문) · content/2026-10-01.json(정본) · content/topics.json(기존 제목과 겹치지 않게)
- (imageConcept가 있으면) 기사 JSON에 "imageConcept": "{imageConcept}" 필드를 넣는다.

[숫자 기준 — 전부 지켜야 끝]
- 분량(공백 제외): sprout 380~450 / lower 700~1000 / upper 1300~1500
- 문체: 해요체. '~습니다/~합니다/~됩니다/~봅시다' 0문장. '~랍니다/~답니다'는 이 신문의 이야기체라 써도 된다.
- 낱말 8개: 앞 4개는 lower 본문과 upper 본문 **둘 다**에, 뒤 4개는 upper에만. upper 본문에 8개 **전부**. sprout 본문에 1~2개.
  본문에 **기본형 그대로**(활용형은 밑줄이 안 뜬다). 활용형만 자연스러운 낱말이면 명사로 바꿔라.
- 학년 간 같은 문장 0 — 세 학년은 각각 새로 쓴다.
- 퀴즈 유형: sprout mc/ox 3~4문항 · lower mc,ox,mc,ox,cloze · upper mc,multi,ox,order,mc · 전부 explain. factbox.sprout · think.sprout 필수.
- cloze: 빈칸은 한 문항에 하나. acceptable은 **빈칸에 넣어 문장이 되는 값만**("하루 ___ 줄"에 "다섯 줄" 금지). 정답이 다른 vocab 낱말 뜻과 겹치지 않게 문항을 좁혀라.
- 사실: 지어낸 수치·연도·인용 금지. 숫자는 node -e로 계산해 확인. 애매하면 "~라고 알려져 있어요". 원작이 있는 이야기는 **원작의 결말·논지를 그대로**.
- 확인: node tools/validate.mjs content/{date}.json → OK, node tools/check-month.mjs {대상 월} → 이 편 ✔
반환: 제목 / 세 단계 글자 수 / 검사 결과 한 줄.
```

전부 끝나면 `node tools/check-month.mjs <대상 월>` → 걸린 편은 그 편 에이전트 없이 **네가 직접** 고친다.

## C. 발행 이력 갱신

`index.json`에 발행일을 순서대로 붙이고, `topics.json.published`에 편마다
`{ date, issueNo, category, badgeLabel, emoji(rotation[slot].emoji), title, keywords(vocab 앞 5개 word), slot }`을 붙이고,
`nextIndex = (마지막 slot + 1) % rotation.length`. → `npm test`가 전부 통과해야 한다(`archive.test.mjs`가 이 정합을 잰다).

## D. 표지 그림

1. `node tools/gen-all-images.mjs 30` — 없는 것만 만든다.
   - 로그에 `HTTP 429` + `spending cap`이 보이면 **표지 단계를 멈춘다**(재시도로 안 풀린다 — 월 지출 상한). PR 「막힌 것」 맨 위에 "표지 N장 미생성 — Gemini 지출 상한. https://ai.studio/spend" 를 적고 E로 넘어간다.
2. **전수 육안 검수** — 에이전트 4갈래로 나눠 한 장씩 Read로 열고, **글자가 박히기 쉬운 부위(화면·간판·포장·종이·러그·깃발·차량 옆면)는 node+sharp로 잘라 확대해** 본다. 불합격 기준:
   - 한글(깨진 자모 포함)·영문·숫자·물음표·느낌표·X·O·체크, 문자처럼 보이는 낙서, 로고
   - 기사와 무관한 범용 그림(10월: 건축 기사에 음악회)
3. 불합격은 파일을 지우고 다시 만든다(`fs.unlinkSync` 후 1번 재실행). **두 번 연속 오염이면 금지 문구를 늘리지 말고 소재를 바꾼다** — 그 편 JSON에 `imageConcept`를 넣되 **글자를 새길 표면(상자·화면·종이·간판·기계 패널)이 아예 없는 장면**으로. 10월 광고 편은 태블릿→상자 앞면→상자 옆면으로 자리만 옮기다가, 봉제 인형·리본·빛만 남기자 한 번에 깨끗해졌다.
4. 세 번째도 불합격이면 그 장은 비워 두고 PR에 적는다.

## E. 원고 1차 검수 → 수정

- 검수 에이전트 4갈래(각 5편 안팎), **보고만 하고 파일은 안 고친다.** 지시에 넣을 것:
  - 전문(세 학년 본문·vocab·factbox·퀴즈 전부)을 다 읽는다
  - **[치명] 사실 오류** — 숫자는 계산. **"~라고 전해져요" 같은 완충 표현이 붙은 대목도 사실 자체를 따로 확인**(10월: '거북 머리'를 완충했다고 칭찬하고 넘겼는데 당시 기록은 용머리였다)
  - **[치명] 퀴즈 오류** — 한 문항씩 **직접 풀어** 정답·보기·해설을 본문과 대조. **학년끼리 서로 모순되지 않는지**(형제가 다른 학년 판을 같이 읽는다)
  - [보통] 부적절한 표현·어법·학년 수준 · [경미]는 정말 값진 것만
  - 형식: 위치(필드 경로) + 원문 인용 + 문제 + 수정안. "깨끗하면 '이상 없음'이 좋은 결과"라고 적어 줄 것(없는 문제를 만들지 않게)
- 수정: **같은 에이전트에게** 이어서 고치게 한다(세부를 쥐고 있다). 수정 지시에 반드시: **"한 곳을 고치면 그 내용이 나오는 모든 곳(세 학년 본문·intro·factbox·vocab·퀴즈 질문·보기·해설·think)을 grep으로 찾아 함께 고쳐라"** — 10월 2차 검수에서 가장 흔한 결함이 "한 곳만 고치고 옆은 옛 그대로"였다.

## F. 원고 2차 검수 → 수정

- **새 에이전트에게, 편 묶음을 섞어서**(1차와 다른 눈이 보게). 초점: ① 1차 수정이 새로 만든 문제(비문·새 문장의 사실 오류·한 곳만 고친 흔적) ② 1차가 놓친 것. 지시·수정 방식은 E와 같다.
- 10월 실적: 1차 치명 11 → 2차 치명 2. **2차를 건너뛰지 않는다.**

## G. 최종 검증 — 전부 통과해야 H로

```
node tools/check-month.mjs <대상 월> --images   (표지가 막혔으면 --images 없이)
npm test
npm run check:print                           (A4 한 면 — 넘치면 그 편 upper를 줄인다)
```

## H. PR 준비 — `.monthly/` 에 세 파일

- `.monthly/PR_TITLE.txt` — 한 줄: `아침신문 <대상 월> 발행분 <첫 호>~<끝 호>호 (N편)`
- `.monthly/PR_BODY.md` — Matto가 **이것 한 장만 읽고** 합칠지 정한다. 순서대로:
  1. **Matto 판단 필요** (없으면 "없음") — 방향이 갈려 보수적으로 둔 것과 그 이유
  2. **막힌 것 / 확인할 것** — 표지 미생성, 공휴일 재확인 결과, 세 번째도 불합격한 표지
  3. **편성표** — 날짜 · 호 · 계열 · 대주제 · 제목 (표)
  4. **검수 결과** — 1차·2차 치명/보통/경미 건수, 치명은 한 줄씩 무엇을 고쳤는지
  5. **표지 미리보기** — 편마다 `![{date}](https://raw.githubusercontent.com/matto12-1/matto-morning-news/{가지}/content/img/{date}.jpg)`
  6. **검증** — check-month·npm test·check:print 결과 한 줄씩
  7. 맺음: "합치면(Merge) master에 들어가 배포되고, 각 편은 발행일 아침에 열립니다."
- `.monthly/DONE` — 빈 파일. **G가 전부 통과했을 때만** 만든다. 이게 있어야 스크립트가 PR을 연다.

## 7. 스스로 정하지 말 것 → PR 「Matto 판단 필요」

- 원작이 있는 이야기의 **논지를 바꾸는 것**, 제목을 편성과 다르게 바꾸는 것, 대주제·소주제 교체
- 사실 여부가 끝까지 불확실한 것(출처가 엇갈림) — 기사에서는 빼거나 완충하고, 무엇이 불확실했는지 적는다
- 기존 발행분(이미 배포된 호)의 오류를 발견하면 **고치지 말고** 적기만 한다

## 시험 모드

프롬프트에 `Mode: test`가 있으면(스크립트의 `-Smoke`): 발행일 **앞 2편만**, 표지는 **그중 1장만**, 원고 검수는 **1차만**. 나머지 절차·기준은 같다. PR 제목 앞에 `[시험]`을 붙인다.
